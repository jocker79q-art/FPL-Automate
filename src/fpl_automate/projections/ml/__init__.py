"""Machine-learned projection model (Phase 2 of docs/ROADMAP.md).

Trained on multi-season historical data and benchmarked against
`projections/baseline_model.py` via a walk-forward backtest (see
`ml/backtest.py`) -- shipped *alongside* the baseline, never as a silent
replacement, per the roadmap.
"""
